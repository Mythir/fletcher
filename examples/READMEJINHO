General:
-Passing the "-h" flag to benchmark script causes a help menu to appear for the CLI
-Num_exp (number of experiments to average results over) was 16 for the AWS benchmarks

Dependencies:
Cython, numpy, pandas, pyarrow, Pyre2

Regex:
-Requires an input file that is generated by the regexp C++ benchmarks.
Install custom c extension module with:
sudo python3.6 setup.py build_ext --inplace
Run with:
sudo python3.6 regexp.py --num_exp <num_exp> --input_strings <path_to_strings_file> --num_proc 144

k-means:
Install custom c extension module with:
sudo python3.6 setup.py build_ext --inplace
Run with:
sudo python3.6 kmeans.py --num_exp <num_exp> --num_rows 15790320 --iteration_limit 25

stringwrite:
Install custom c extension module with:
sudo python3.6 setup.py build_ext --inplace
Run with:
sudo python3.6 stringwrite.py --num_exp <num_exp> --num_strings 8388608

filter:
-Requires an input file that is generated by the filter C++ benchmarks on Johan's repo. This requires setting "save_to_file" to true in the code. The C++ code on my fork is not up2date.
Install custom c extension module with:
sudo python3.6 setup.py build_ext --inplace
Run with:
sudo python3.6 filter.py --num_exp <num_exp> --input_strings <path_to_strings_file>

